namespace ProcessManager.Domain.Enums;

public enum StepContentType
{
    Text,
    Image,
    /// <summary>An operator data-entry prompt (numeric, pass/fail, multiple-choice, etc.).</summary>
    Prompt
}
