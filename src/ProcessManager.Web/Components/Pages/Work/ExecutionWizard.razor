@page "/execute/{StepExecutionId:guid}"
@rendermode InteractiveServer

<ExecutionWizardContent StepExecutionId="@StepExecutionId" />

@code {
    [Parameter] public Guid StepExecutionId { get; set; }
}
